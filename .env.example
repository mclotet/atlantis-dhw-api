# Required secret — never goes in atlantis.toml
DHW_INFLUXDB_TOKEN=your-token-here

# Optional runtime overrides (non-secret defaults live in atlantis.toml)
# ATL_ENV=itg
# ATL_LOG_LEVEL=DEBUG
ATL_DHW_EDGE_NODE_ID=rack
